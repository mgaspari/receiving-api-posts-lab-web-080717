<h1>New Product</h1>
<%= form_for @product do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br />
  <%= f.label :price %>
  <%= f.text_field :price %>
  <br />
  <%= f.label :description %>
  <%= f.text_area :description %>
  <br />
  <%= f.label :inventory %>
  <%= f.text_field :inventory %>
  <%= f.submit "submit" %>
<% end %>
<p id="put_here"></p>


<script type="text/javascript" charset="utf-8">
$(function () {
  $('#new_product').submit(function(e){
    e.preventDefault()
    var values = $(this).serialize();

    var posting = $.post('/products', values);
    posting.done(function(data) {
      console.log(data)
      $('#put_here').text(data["name"])
      // $('#put_here').text(`$${data["price"]}`)
      //  ${data["price"]} ${data["inventory"]} ${data["description"]}
      //can display on page
      });
      // e.preventDefault()
      // var values = JSON.stringify($(e.target).serializeArray())
      // // debugger
      // fetch("/products.json", {
      //   method: "POST",
      //   body: values
      // } ).then( res => res.json()).then(res => console.log(res))
  })
});
</script>
